//http://play.elevatorsaga.com/

Mais proximo {

	If(Call > current) and D(1)
	Target < Current

}
6 - > 8, 3 - > 0, 2 - > 1

Building(NumElevadores, NumAndares) {
	let Andares = [];
	let Elevadores = [];
	ConstruirPredio(NumElevadores, NumAndares) {
		//AdicionarAndares
		//AdicionarElevadores
	}

}
Fila[5 - > 1]
AdicionarChamada() {
	Fila.add(Floor)
	ChecaFila();
}
ChecaFila() {
	While(Fila > 0) {
		ProcuraElevador(Fila[0])
	}
}
}

ProcuraElevador(6 - > Up) {
	//Ordernar por mais proximo.

	for (Elevadors)
		if (Elevador[i].AceitaChamada(6 - > Up))
			Fila.pop();
	break

}

Elevator(Numero, Capacidade) {
	let CurrentFloor = 2;
	let DestinationFloor = 2;
	let DestinationFloors = [];
	let people = 0;
	let Direction = None;
	let Statistics;
	//let DirectionCalled = None;

	//function GetDirection();

	//--function IsOccupied();

	function IsDoorOpened();

	function OpenDoor();

	function CloseDoor();

	function StartMoving();

	function StopMoving();

	function UpdateMoving();

	function Move();

	function addDestination();

	function Draw();

	function IsFloorOnTheWay();



	GetPerson(targetperson) {
		Move()
	}
	ChangeDirection() {
		if (people[] === 0)
			DirectionCalled();
	}
	AceitoChamada(3 - > Person.Direction) {
		if (Direction === none) {
			DestinationFloors.push(5 - > Down);
			Move();
			return true

		} else {
			if ((CurrentFloor < 3 and Direction.UP) || CurrentFloor > 6 and Direction.Down)) {
			if (DirectionCalled === Person.Direction) {
				DestinationFloors.push(3 - > Down);
				return true;
			}
		}
	}


	Move() {
		MovimentarElevador();
		if (currentFloor in DestinationFloors[5]) || (currentFloor in people[{
			5 - > 1
		}]) {
			GetPerson()
			Remove()
		}
		If(people || CalledFloor)
		Move();
		else {
			//limpar variaveis
			Direction = None
			DirectionCalled = None
		}
	}



}
